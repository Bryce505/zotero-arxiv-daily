name: CMC literature monthly synthesis

on:
  workflow_dispatch:
  schedule:
    # 13:00 UTC on the 1st = 21:00 Asia/Shanghai, synthesising the month just
    # ended. Running mid-month would miss a fifth Friday landing on the 29th-31st.
    - cron: '0 13 1 * *'

permissions:
  contents: write

jobs:
  synthesis:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Sync dependencies
        run: uv sync

      - name: Build and send the monthly synthesis
        env:
          ZOTERO_ID: ${{ secrets.ZOTERO_ID }}
          ZOTERO_KEY: ${{ secrets.ZOTERO_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_API_BASE: ${{ secrets.OPENAI_API_BASE }}
          SENDER: ${{ secrets.SENDER }}
          SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD }}
          RECIPIENTS: ${{ secrets.RECIPIENTS }}
          CUSTOM_CONFIG: ${{ vars.CUSTOM_CONFIG }}
        run: |
          printf "%b\n" "$CUSTOM_CONFIG" > config/custom.yaml
          uv run src/zotero_arxiv_daily/monthly.py

      - name: Push the archived synthesis
        run: git push || echo "nothing to push"
